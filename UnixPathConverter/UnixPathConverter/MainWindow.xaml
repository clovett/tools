<Window x:Class="UnixPathConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UnixPathConverter"
        mc:Ignorable="d"
        Title="Unix Path Converter" Height="350" Width="525" Foreground="White">
    <Grid Background="#303030">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Windows:" Margin="10"/>
        <TextBox x:Name="WindowsPath" Grid.Row="1" Margin="10" TextChanged="OnWindowsPathChanged" Padding="2"/>
        <TextBlock Text="Unix:" Grid.Row="2" Margin="10"/>
        <TextBox x:Name="UnixPath" Grid.Row="3" Margin="10" TextChanged="OnUnixPathChanged" Padding="2"/>
    </Grid>
</Window>
