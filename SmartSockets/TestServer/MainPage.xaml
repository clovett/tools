<Page
    x:Class="TestServer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestServer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Background="#CCEECC">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Server:" Margin="10" VerticalAlignment="Center"/>
            <TextBox x:Name="SendText" Margin="8" Grid.Column="1" Text="" KeyDown="OnSendTextKeyDown"/>
        </Grid>

        <RichEditBox x:Name="TestOutput" ScrollViewer.VerticalScrollBarVisibility="Auto" Grid.Row="1">
        </RichEditBox>

        <Border Background="#CCEECC" Grid.Row="2"  >
            <TextBlock x:Name="Status" Margin="10" />
        </Border>
    </Grid>
</Page>
